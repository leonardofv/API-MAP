import s from './footer.module.scss';

export default function Footer() {
    return (
        <footer className={s.footer}>
            <h4 className={s.title}>Atividade de fixação vai na web</h4>
        </footer>
    )
}